apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd
  namespace: argocd
spec:
  project: default
  sources:
    - chart: argo-cd
      repoURL: https://github.com/sekthor/tf-lab
      path: argo
      targetRevision: HEAD
      helm:
        releaseName: argocd
  destination:
    server: "https://kubernetes.default.svc"
    namespace: argocd
  syncPolicy:
    automated: 
      prune: true
      selfHeal: true